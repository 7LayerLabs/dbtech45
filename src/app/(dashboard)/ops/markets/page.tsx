import Terminal from '@/components/terminal/Terminal';

export const metadata = {
  title: 'DB Terminal',
  description: 'Bloomberg-style trading terminal — ORB, sectors, options flow, news wire, journal, AxeCap.',
};

export default function MarketsPage() {
  return <Terminal />;
}
